# MongoDB Connection Options (choose one):

# Option 1: MongoDB Atlas (Cloud)
# ATLAS_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/<database-name>?retryWrites=true&w=majority

# Option 2: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/chatx

# Option 3: If no URI is provided, defaults to local MongoDB
# The server will automatically use: mongodb://localhost:27017/chatx

# Server Configuration
PORT=3001

# Quick Start Instructions:
# 1. For local development, just run: brew install mongodb-community (on Mac)
# 2. Start MongoDB: brew services start mongodb-community
# 3. No .env configuration needed - server will use local MongoDB by default
# 4. For production, set ATLAS_URI with your MongoDB Atlas connection string
